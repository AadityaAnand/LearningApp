{
  "name": "micro-learning-platform",
  "version": "1.0.0",
  "description": "A micro-learning platform for developers to learn new technologies through bite-sized lessons",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd backend && npx nodemon server.js",
    "start-frontend": "cd frontend && npm start",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": [
    "micro-learning",
    "education",
    "coding",
    "react",
    "nodejs"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.10.0"
  }
}
